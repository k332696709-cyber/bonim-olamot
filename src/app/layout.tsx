// Root layout required by Next.js App Router.
// The [locale] layout below provides <html> and <body>.
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children
}
